name:  Integration tests

on:  workflow_dispatch


jobs:
  linux:

    runs-on: ubuntu-latest
    
    env:
      singleFileExec:  delta-single-file.exe
      os:  linux
      dotnetOs:  linux-x64

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    # # Copy local app-setting files
    # - name:  Copy app setting file
    #   run: |
    #     cp deployment/appsettings.$os.json code/DeltaKustoFileIntegrationTest/appsettings.local.json
    # See https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-restore
    - name: Install dependencies
      run: dotnet restore code
    # See https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-build
    - name: Build
      run: dotnet build code --configuration Release --no-restore
    # See https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-test
    - name: Unit Tests
      run: dotnet test code/DeltaKustoUnitTest --configuration Release --no-build --verbosity normal
    # See https://github.com/dotnet/designs/blob/main/accepted/2020/single-file/design.md#user-experience
    # & https://docs.microsoft.com/en-us/dotnet/core/deploying/single-file
    # & https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-publish
    - name: Build single file executable
      run: |
        dotnet publish code/delta-kusto -c release -r $dotnetOs \
          -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true \
          -p:PublishTrimmed=true -p:PublishReadyToRun=true \
          -o $singleFileExec
    # Set environment variables
    - name: Set environment variables
      run: |
        # Exec path
        echo "deltaKustoSingleExecPath=../$singleFileExec" >> $GITHUB_ENV
        # ADX integration
        echo "deltaKustoClusterUri=${{ secrets.CLUSTER_URI }}" >> $GITHUB_ENV
        echo "deltaKustoCurrentDb=${{ secrets.CURRENT_DB }}" >> $GITHUB_ENV
        echo "deltaKustoTargetDb=${{ secrets.TARGET_DB }}" >> $GITHUB_ENV
        echo "deltaKustoTenantId=${{ secrets.TENANT_ID }}" >> $GITHUB_ENV
        echo "deltaKustoSpId=${{ secrets.SP_ID }}" >> $GITHUB_ENV
        echo "deltaKustoSpSecret=${{ secrets.SP_SECRET }}" >> $GITHUB_ENV
    # See https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-test
    - name: File-based integration tests (out-of-proc)
      run: dotnet test code/DeltaKustoFileIntegrationTest --configuration Release --no-build --verbosity normal
    # See https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-test
    - name: ADX-based integration tests (out-of-proc)
      run: dotnet test code/DeltaKustoAdxIntegrationTest --configuration Release --no-build --verbosity normal

  # windows:

  #   runs-on: windows-latest

  #   env:
  #     singleFilePath:  bin/delta-single-file.exe
  #     os:  windows
  #     dotnetOs:  win-x64

